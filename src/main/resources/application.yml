server:
  port: 8080
  
app:
  name:lead-onboard-service
datasource:
  name: postgresql  

spring.jpa:
  database: POSTGRESQL
  hibernate.ddl-auto: update
  show-sql: true

spring:
    datasource: 
      database:  postgres
      driverClassName: org.postgresql.Driver
      url: jdbc:postgresql://localhost:5432/MyDatabase
      username: postgres
      password: ROOT